export default {
  setResistances(state, resistances) {
    state.resistances = resistances;
  },
  setSelected(state, resistance) {
    state.selected = resistance;
  },
  setSelectedProtesto(state, protesto) {
    state.selectedProtesto = protesto;
  },
};
